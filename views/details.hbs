
<body>
    <header>
        <nav>
            <ul>
                <li>
                    <a id="home" class="left-floated" href="#">MoneyGone</a>
                </li>
            </ul>
            <ul>
                <li>
                    <a class="right-floated" href="#">[myusername profile]</a>
                </li>

                <li>
                    <a class="right-floated" href="#">Logout</a>
                </li>
            </ul>
        </nav>
    </header>

    <main>

        <section class="expense-report">

            <h1>ID: {{product._id}}</h1>

            <div class="report-category">
                <img src="/images/amount.png" alt="">
                <p>{{product.category}} - лв {{product.total}}</p>
            </div>

            <div class="report-merchant">
                <p>{{product.name}}</p>
            </div>

            <div class="report-description">
                <img src="/images/hastag.png" alt="">
                <p>{{product.description}}</p>
            </div>
            <div class="report-action">
                <a disabled href="/delete/{{product._id}}">Stop tracking</a>
            </div>
        </section>
    </main>

</body>

</html>